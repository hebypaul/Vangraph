/**
 * Default context values for development (no auth required)
 */

// Default workspace ID for mock/dev data
export const DEFAULT_WORKSPACE_ID = 'ws-1';

// Default project ID for mock/dev data
export const DEFAULT_PROJECT_ID = 'proj-1';

// Default project key
export const DEFAULT_PROJECT_KEY = 'VAN';
